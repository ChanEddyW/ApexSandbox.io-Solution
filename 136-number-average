public Decimal average(List<Decimal> numbers){
    Decimal avg = 0;

    for (Decimal num : numbers) {
        avg += num;
    }

    avg /= numbers.size();

    avg = avg.setScale(2);

    return avg;
}